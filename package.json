{
  "name": "quantum-safe-file-sharing",
  "version": "1.0.0",
  "description": "Quantum-safe encrypted file sharing application",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "quantum-safe",
    "encryption",
    "file-sharing"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
